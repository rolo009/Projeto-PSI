suite_namespace: frontend\tests\unit
actor: UnitTester
modules:
  enabled:
    - Yii2:
        part: [ orm, email, fixtures ]
    - Asserts
    - Db:
        dsn: 'mysql:host=localhost;dbname=dbcultravel_test'
        user: 'root'
        password: ''
        cleanup: false
        dump: '../frontend/tests/_data/user-insert.sql'
        populate: true # run populator before all test
        populator: 'mysql -u $user $dbname < ../frontend/tests/_data/user-insert.sql'
