suite_namespace: frontend\tests\unit
actor: UnitTester
modules:
  enabled:
    - Yii2:
        cleanup: false
        part: [ orm, email, fixtures ]
    - Asserts
    - Db:
        dsn: 'mysql:host=localhost;dbname=dbcultravel_test'
        user: 'root'
        password: ''
        dump: 'tests/_data/user-insert.sql'
        populate: true # run populator before all test
        populator: 'mysql -u $user $dbname < tests/_data/user-insert.sql'
